<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Login</title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH"
      crossorigin="anonymous"
    />
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap-icons/font/bootstrap-icons.css"
      rel="stylesheet"
    />
    <link rel="stylesheet" href="/mystyles.css" />
  </head>
  <body>
    <div class="container-fluid page-container align-content-center">
      <div class="row d-flex">
        <div class="col-6 d-flex align-items-center">
          <div class="card shadow mx-auto" style="width: 35rem">
            <div class="card-body">
              <!-- Show error message for wrong login credentials-->
              <% if (unauthorised) { %>
              <div
                class="alert alert-danger alert-dismissible fade show mt-3"
                role="alert"
              >
                <strong><%- unauthorised %></strong>
                <button
                  type="button"
                  class="btn-close"
                  data-bs-dismiss="alert"
                  aria-label="Close"
                ></button>
              </div>

              <% } %>

              <h3 class="card-title mb-3 mt-5"><strong>Login</strong></h3>
              <h5 class="card-title mb-5">
                Don't have an account yet? <a href="/register">Sign Up</a>
              </h5>
              <form method="post" action="/login">
                <div class="mb-3">
                  <label for="email" class="col-form-label"
                    ><strong>Email Address</strong></label
                  >
                  <input
                    class="form-control form-control-lg mb-3"
                    type="text"
                    placeholder="name@example.com"
                    name="email"
                  />
                </div>
                <div class="mb-3">
                  <label for="password" class="col-form-label"
                    ><strong>Password</strong></label
                  >
                </div>

                <!-- Show error if session contains error -->
                <% if (passwordmatch) { %>
                <div
                  class="alert alert-danger alert-dismissible fade show mt-3"
                  role="alert"
                >
                  <strong>Password doesn't match. Please enter password.</strong>
                  <button
                    type="button"
                    class="btn-close"
                    data-bs-dismiss="alert"
                    aria-label="Close"
                  ></button>
                </div>
                <% } %>

                <input
                  class="form-control form-control-lg mb-3"
                  type="password"
                  placeholder="Enter Password"
                  name="password"
                />

                <div class="d-grid mt-5">
                  <button type="submit" class="btn btn-success btn-lg">Login</button>
                </div>
              </form>
            </div>
          </div>
        </div>
        <div class="col-6 d-flex mx-auto align-items-center">
          <img
            src="/Images/TaskManagerLoginImage.png"
            alt="Task Manager Logo"
            class="img-fluid"
            style="max-height: 60vh"
          />
        </div>
      </div>
    </div>

    <script
      src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"
      integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz"
      crossorigin="anonymous"
    ></script>
  </body>
</html>
